# TODO: axe this file in favor of EnsureOperatorConfigExists within the operator
apiVersion: operator.openshift.io/v1
kind: ServiceCA
metadata:
  name: cluster
spec:
  managementState: Managed
  version: 4.0.0
  logLevel: "4"
  replicas: 1
